apiVersion: 1

providers:
  - name: 'EcoFirma Dashboards'
    orgId: 1
    folder: 'EcoFirma'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
